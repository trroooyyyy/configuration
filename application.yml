spring:
    datasource:
        username: trroooyyyy
        password: "06062023"
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
eureka:
    client:
        service-url:
            defaultZone=http://localhost:8761/eureka/:
